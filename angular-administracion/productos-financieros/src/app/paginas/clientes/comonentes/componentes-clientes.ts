/**
 * Archivo que contiene los componentes que se van a utilizar en el componente Clientes
 */

import { TablaClientesComponent } from '../../../componentes/tabla/clientes/tabla-clientes/tabla-clientes.component';

export const componentesClientes = [TablaClientesComponent];
